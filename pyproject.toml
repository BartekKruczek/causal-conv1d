[build-system]
requires = ["setuptools", "wheel", "ninja", "packaging"]
build-backend = "setuptools.build_meta"

[tool.uv.extra-build-dependencies]
causal-conv1d = [{ requirement = "torch", match-runtime = true }]